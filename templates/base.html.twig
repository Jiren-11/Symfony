<!DOCTYPE html>
<html>
	<head>
		<meta
		charset="UTF-8">
		{# lien css bootstrap #}

		<title>
			{% block title %}Welcome!
			{% endblock %}
		</title>
		<link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
		{# <link rel="stylesheet" href="{{asset("")}}"> #}
		{# lien du fichier css dans le dossier public puis le fichier css #}
		<link rel="stylesheet" href="{{asset("css/style.css")}}"> 
		<link rel="stylesheet" href="{{asset("css/accueil.css")}}"> 
		{# <link rel="stylesheet" href="{{asset("css/bootstrap.css")}}">  #}
		{# <link rel="stylesheet" href="css/bootstrap.css"> #}
		{# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
			{% block stylesheets %}
		{{ encore_entry_link_tags('app') }}

		{% endblock %}

			{% block javascripts %}
		{{ encore_entry_script_tags('app') }}
	{% endblock %}
	</head>
		<body>


			<nav>
				<div class="logo"> <a href="#">
					<img src="{{ asset('img/logow.png') }}" style="">
				</a>
			</div>
			<ul>
				<li style ="    position: fixed;
    							top: 0;
    							left: 150px;">
					<a href="{{ path('app_accueil') }}"class="o">Accueil</a>
				</li>

				{% if is_granted('ROLE_ADMIN') %}
					<li style = "position: fixed;
    							top: 0;
    							left: 300px;">
						<a href="{{ path('app_devis_crud_index') }}"class="o">Gestion Devis</a>
						
						{% endif %}
				</li>
				{% if is_granted('ROLE_ADMIN') %}
					<li style ="position: fixed;
    							top: 0;
    							left: 485px;">
						<a href="{{ path('app_haie_index') }}"class="o">Gestion des Haie</a>
						<ul>
							<li><a href="{{path('app_haie_new')}}"class="o">Création Haie</a></li>
						</ul>
					</li>
					<li style ="position: fixed;
    							top: 0;
    							left: 700px;">
						<a href="{{ path('app_user_crud_index') }}"class="o">Gestion des clients</a>
					</li>
				{% endif %}
				{% if app.user == null %}
					<li>
						<a href="{{path('app_register')}}"class="o">Inscription</a>
					</li>

					<li>
						<a href="{{ path('app_login') }}" class="logout-btn">
					<lord-icon src="https://cdn.lordicon.com/dxjqoygy.json" trigger="hover" colors="primary:#ffffff,secondary:#ffffff" style="width: 40px; height: 40px;"></lord-icon>
    					Connexion
  					</a>
					<script src="https://cdn.lordicon.com/bhenfmcm.js"></script>
				</li>
				{% endif %}

				{% if is_granted('ROLE_USER') %}
				<li>
  					<a href="{{ path('app_logout') }}" class="logout-btn">
    					<lord-icon src="https://cdn.lordicon.com/moscwhoj.json" trigger="hover" colors="primary:#ffffff,secondary:#ffffff" style="width: 40px; height: 40px;"></lord-icon>
    					Déconnexion
  					</a>
					<script src="https://cdn.lordicon.com/bhenfmcm.js"></script>
				</li>

				{% endif %}
			</ul>
			</nav>
		
		{# <script>
			window.addEventListener('scroll', function() {
    			var nav = document.querySelector('nav');
    			nav.classList.toggle('sticky', window.scrollY > 0);
			});
		</script> #}


		</body>
	</body>
</html></body></html></ul>{% block content %}{% endblock %}</html></body></html>
